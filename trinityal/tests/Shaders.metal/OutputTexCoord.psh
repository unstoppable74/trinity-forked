#include "MetalDefines.h"

#include <metal_stdlib>
using namespace metal;

struct VS_OUTPUT
{
	float4 Position [[ position ]];
	float2 TexCoord;
};

fragment float4 mainPS(VS_OUTPUT In [[ stage_in ]])
{
    float4 color = float4( In.TexCoord, 0, 1 );
    return color;
}
