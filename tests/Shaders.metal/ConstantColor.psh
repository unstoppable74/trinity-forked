#include "MetalDefines.h"

#include <metal_stdlib>
using namespace metal;

struct VS_OUTPUT
{
	float4 Position [[position]];
};

fragment float4 mainPS(VS_OUTPUT In [[stage_in]] SHADOW_PS_DECL)
{
    float4 color = float4( 1, 0, 0, 1 );
    return color;
}
